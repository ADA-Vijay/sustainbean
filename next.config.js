/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com",'images.forbes.com','image.savethechildren.org','themewagon.github.io'],
  },
};

module.exports = nextConfig;
